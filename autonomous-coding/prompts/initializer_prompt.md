You are bootstrapping a new autonomous coding project with Codex.

Create the initial workspace structure for a small but real application.
Use the app specification in `app_spec.txt` as the product target.

Requirements:

- Create a `feature_list.json` file with exactly `{{FEATURE_COUNT}}` feature entries.
- Each feature should be concrete, testable, and ordered from foundational to advanced.
- Create an `init.sh` script that installs dependencies and starts the application.
- Create a project README with setup and run instructions.
- Initialize the repository history in the generated project if needed.
- Preserve any existing files that are already present unless you need to expand them intentionally.
- Keep the project ready for a later coding session to continue from the feature list.

Output:

- Write the files directly in the workspace.
- Prefer clear, minimal scaffolding over overengineering.
- Keep the output tied to OpenAI APIs and this Codex quickstart.
